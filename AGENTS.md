Keep shared Bicep templates reusable and deployment-specific values in parameter files; preserve configured custom domains and TLS bindings when fixing deployments.
Keep application releases limited to application and container-build changes, as documented in CONTRIBUTING.md.
